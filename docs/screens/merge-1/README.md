# merge-1 — brand and shell

Wed 16 Sep 2026. Every screen at 390×844 in light, the five tabs also in dark
and Arabic, plus the generated PWA icon.

This pass changed tokens, fonts, the header, the tab bar and the icons. Screen
layouts are untouched except Home's hero, which had to stop being a coloured
slab: re-skinning by token alone would have turned a navy hero red, and a
full-bleed red page is the one thing the brand rules forbid. It is paper now,
with the red on the logo tile where it belongs. Phase 3 rebuilds Home properly.

`radar-*.png` is the Radar tab landing on Known Threats. Phase 2 folds the
documented campaigns into it; until then the tab reaches a real screen rather
than a placeholder.
