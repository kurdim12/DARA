# public/brand

`dara-mark.png` belongs here — it is copied from `reference/brand/dara-mark.png`
by `npm run brand:sample`. Until then the app renders the word درع as plain
text rather than a lookalike, and the icons in `public/icons/` are blank
placeholders in the paper colour.
